[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "*"
black = "*"
pytest-pretty = "*"

[requires]
python_version = "3.11"

[scripts]
test = "pytest ."
format = "black ."
autolint = "ruff check . --fix"
lint = "bash -c \"black . --check && ruff check .\""
